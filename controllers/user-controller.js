module.exports = class UserController {

    static findProperties(req, res){

        const longtidude = req.query.long;
        const latidude = req.query.latidude;

        //call helper

    }

    static execBookingRequest(req, res){

        const propertyObject = req.query.prop;

        //call helper

    }

}